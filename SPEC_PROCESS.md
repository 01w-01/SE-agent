# FBW Coding Agent Harness 规约过程记录

## 1. 文档目的与证据范围

本文记录使用 Superpowers `brainstorming` 形成正式 `SPEC.md` 的过程，重点保留问题、方案变化、人工判断和反思，而不是把最终设计重复一遍。

证据来源：

- 课程原文：`通用要求.md`、`AI4SE_Final_Project_A_Coding_Agent_Harness.md`；
- 第 1–18 轮主对话及 `docs/LOG.md`；
- brainstorming 设计依据：`docs/superpowers/specs/2026-08-08-coding-agent-harness-demo-design.md`；
- Git 提交 `77da924`、`b773647`；
- 2026-08-08 完成的正式 `SPEC.md`。

本文件是持续更新的过程证据。PLAN 尚未获准生成，陌生智能体冷启动也尚未进入可执行门禁；对应状态见 §8，而不是伪造已完成记录。

## 2. Brainstorming 的关键问题

brainstorming 主要推动了以下问题从模糊想法变成可验证决定：

1. 成品还是局部机制样例：只有 `clamp()` 修复不足以成为可使用产品，最终确定“真实 CLI 产品 + Mock 机制演示”双轨交付。
2. 哪个维度做深：从宽泛的“反馈闭环”收敛到 pytest 失败的采集、结构化、回灌、重试和停机。
3. 如何证明闭环不是提示词表演：真实与 Mock LLM 共用同一 AgentLoop，Mock 只替换模型决策源。
4. 如何直接修改又保护用户文件：不用全仓复制，改用工作区围栏、动作白名单、风险分级、逐文件事务和失败回滚。
5. 哪些行为需要人工介入：普通受控操作自动执行，高风险操作确认，越界、删除和任意 shell 永久拒绝。
6. 如何在初学者可理解与工程深度间平衡：采用模块化单进程状态机，避免单文件耦合，也不引入事件总线和多进程。
7. “可舍弃仓库”是否等于“玩具项目”：用户最终纠正为正式课程流程，设计依据必须升级成完整 SPEC、过程文档、分发和 CI 约束。

其中第 1、4、7 项直接修正了早期方案，最能体现 brainstorming 的价值。

## 3. 关键迭代

### 3.1 迭代一：从语言陌生到 Python + uv

- 初始问题：机器上 Python/uv、Node.js、Go 均可作为候选，但用户对三种语言都不熟悉。
- 对话节选（意译）：用户表示“如果让我选，肯定选 Python + uv”，并要求用白话解释打包和跨平台。
- AI 建议：Python 适合快速实现状态机、pytest 集成和 OpenAI 兼容 API；uv 管 Python 与依赖，PyInstaller 另负责 Windows `.exe`。
- 人工决策：采纳 Python + uv，不把 uv 错当打包器；Windows 优先，最终用户不需要安装 Python。
- 规约影响：`SPEC.md` 固定 Python、uv、pytest、PyInstaller 和 Windows x64 发布路径。

### 3.2 迭代二：从“反馈闭环”口号到可测信号

- 初始问题：六个 harness 维度中想突出反馈，但尚无客观信号和确定性场景。
- 对话节选（意译）：用户先选择“单元测试失败”，再接受 `clamp()` 边界错误作为最小任务，并选择结构化反馈。
- AI 建议：形成“写入 → pytest 失败 → 结构化 Feedback → 回灌 → 不同动作 → pytest 通过”的闭环；用失败指纹识别无进展。
- 人工决策：采纳单元测试失败、`clamp()` 场景和结构化反馈；任务本身保持简单，把深度放在反馈机制。
- 规约影响：反馈字段、完成门禁、重复失败停机和 Mock 演示成为核心验收标准。

### 3.3 迭代三：纠正 demo 的产品范围

- 初始问题：AI 一度把 demo 解释成只展示 `clamp()` 修复的局部机制样例。
- 对话节选（意译）：用户指出自己要的是“可以导入 Base URL 和 API Key 后直接使用，只是比较简陋”的成品。
- AI 修正：拆成两个互补部分：真实 LLM 驱动的可用 CLI，以及不依赖网络的 Mock LLM 机制演示。
- 人工决策：选择“简单但能真正运行”，同时保留 `clamp()` 作为课程证据，而非产品唯一功能。
- 规约影响：加入真实 API 任务、凭据生命周期、发布物、README 和冷启动验收。

### 3.4 迭代四：推翻粗粒度安全选项

- 初始问题：曾提出全仓临时副本、受限演示、直接修改三种安全级别，但选项没有说清大仓库成本和直接修改风险。
- 对话节选（意译）：用户明确反对复制大型项目，并认为“方案 C 不符合不能损坏其他文件”的说法过于模糊，无法放心选择。
- AI 修正：把安全拆成工作区边界、动作白名单、ALLOW/CONFIRM/DENY、写前哈希、原子替换、逐文件恢复和明确非沙箱边界。
- 人工决策：选择直接修改原项目；普通受控修改自动执行，仅高风险审批；任意 PowerShell、删除和越界永久禁止。
- 规约影响：安全不再依赖整仓复制，而由多层可单测机制共同保证。

### 3.5 迭代五：选择模块化单进程状态机

- 初始问题：比较单文件线性循环、模块化状态机、事件驱动插件内核。
- AI 建议：方案 B 能隔离循环、策略、事务、测试与反馈，同时不引入异步和插件生命周期。
- 人工决策：采纳方案 B；用户将普通内部技术细节委托给 AI 推荐，但保留产品行为和风险决策权。
- 规约影响：正式 SPEC 明确模块职责、状态迁移、统一 LLM 接口和数据流。

### 3.6 迭代六：从探索仓库升级为正式课程项目

- 初始问题：早期对话和设计文档多次使用“探索 demo”，容易被理解为无需完整课程交付。
- 对话节选（意译）：用户纠正“仓库可以舍弃，但本次流程必须按正式课程项目执行，最后要做出可运行成品”。
- AI 处理：不重做 brainstorming，而是把课程原文、已确认设计和冲突合并为正式 SPEC，并建立批准门禁。
- 人工决策：要求先完成、自检并审阅 SPEC；批准前禁止 writing-plans 和实现。
- 规约影响：加入完整分发、CI、凭据威胁模型、六维度最低实现、课程过程和冲突登记。

### 3.7 迭代七：CLI 可扩展边界与正式 Git 工作流

- 初始问题：正式 SPEC 把“不做 WebUI”和“核心结构”写得过于接近，也把早期“在 main 推进”的决定延续到了正式编码阶段。
- 对话节选（意译）：用户确认首版纯 CLI，但要求结构允许未来添加 WebUI；现有 Git 历史不动，正式编码开始采用 branch + worktree + PR。
- AI 建议：不创建空 WebUI 占位代码，而以 CLIAdapter、ApplicationService、EventSink 和 ApprovalProvider 形成可替换入口边界；已有文档提交留在 main，未来 PLAN task 隔离开发。
- 人工决策：采纳界面解耦方案和课程推荐 Git 工作流；临时 Key 风险在当前私有阶段接受，历史不重写。
- 规约影响：首版仍是纯 CLI，但核心不得依赖终端；C-03 标记已解决，凭据问题降为最终发布/提交门禁。

## 4. AI 建议与人工处理汇总

### 4.1 采纳的建议

| AI 建议 | 人工处理 | 原因 |
|---|---|---|
| Python + uv，PyInstaller 单独打包 | 采纳 | 学习成本和 pytest/API 生态适合本项目 |
| 单元测试失败作为主反馈信号 | 采纳 | 客观、可重复、容易确定性测试 |
| `clamp()` 失败—反馈—修正演示 | 采纳 | 场景简单，可把评审注意力放在机制 |
| 结构化 Feedback 和稳定指纹 | 采纳 | 同时利于人读、模型消费和无进展判断 |
| 模块化单进程状态机 | 采纳 | 兼顾清晰边界与首版复杂度 |
| Windows Credential Manager | 在正式 SPEC 阶段采纳 | 满足安全存储和新用户引导要求 |
| Windows x64 单文件发行物 | 采纳 | 符合纯 CLI 和免 Python 使用目标 |

### 4.2 推翻或修正的建议

| 原建议/表达 | 人工处理 | 修正结果 |
|---|---|---|
| 把 demo 收窄为单个 `clamp()` 任务 | 推翻 | 成品是通用受控 CLI，`clamp()` 仅作机制演示 |
| 用全仓临时副本作为主要安全方案 | 推翻 | 使用直接修改、逐文件事务、围栏和回滚 |
| 三档安全方案的粗粒度描述 | 要求重做 | 分解为动作、路径、风险、事务、停机多个可测机制 |
| 一次只问一个微小问题 | 要求提速 | 简单技术选择由 AI 推荐，集中询问真正改变方向的问题 |
| 探索 demo 可采用简化课程流程 | 推翻 | 仓库可舍弃不等于过程可省略，按正式课程项目执行 |
| 使用分支/worktree 的标准流程 | 先偏离、后修正 | 现有文档历史保留；正式编码起按 branch + worktree + PR 执行 |
| 最终必须 WebUI | 首版不做但修正架构 | CLI 是适配器，核心为未来 WebUI 留接口；当前仍未满足 WebUI 交付项 |

## 5. 对 brainstorming 的反思

### 5.1 做得好的地方

- 把“反馈闭环”转成了可观察、可注入失败、可重复验证的机制，而不是抽象口号。
- 用三种架构比较帮助明确了为什么选择模块化状态机，而不是仅凭偏好定方案。
- 安全方案受到质疑后继续拆解，最终形成能单元测试的规则、事务和停机条件。
- 分节确认让用户及时纠正“demo”的真实含义，避免直接实现错误产品。

### 5.2 不满意的地方

- 早期问题过细，一轮只决定一个小点，使节奏慢，也把不必要的技术判断压力转给基础较弱的用户。
- 最初把 demo 理解得过窄，混淆了“可运行成品”和“机制演示样例”。
- 第一版安全选项粒度不一致：全仓复制的成本没有充分解释，直接修改的保障也没有展开，导致选项不可比较。
- 已经出现凭据历史冲突时，早期仍以探索仓库为由弱化了正式合规影响；正式 SPEC 必须把它视为发布阻断项。

### 5.3 后续改进

- 产品方向问题逐项确认；常规实现细节给推荐默认值和可验证依据。
- 每个选项都同时说明收益、成本、失效边界和课程合规影响。
- 明确区分产品路径、课程机制演示、流程证据三种交付物。
- 用 SPEC 验收标准替代“用户凭技术直觉批准内部实现”的负担。

## 6. 从 brainstorming 设计到正式 SPEC 的变化

| 设计依据 | 正式 SPEC 增补或强化 |
|---|---|
| 可运行 demo 范围 | 正式问题陈述、目标用户、价值、非目标 |
| 基本架构与闭环 | 10 个 INVEST 故事、12 个逐项功能规约、数据关系 |
| 路径和回滚安全 | 凭据威胁模型、Credential Manager、公开发布阻断条件 |
| 三项 Mock 演示 | 六维度最低实现、机制类别、24 条验收标准 |
| Windows CLI | PyInstaller 发行物、GitLab CI、GitHub Release、全新机器路径 |
| “探索 demo”限制 | 正式课程过程、学术诚信、风险、冲突与有意偏离 |

原设计提交 `b773647` 保留为过程证据，但不替代 `SPEC.md`。

## 7. 当前冲突的过程结论

- WebUI：首版坚持纯 CLI，但核心入口与终端解耦，允许未来新增 WebUI；当前仍未实际满足课程 WebUI URL 项。
- 凭据历史：提交 `77da924` 已含临时 Key；用户接受当前私有阶段风险且不重写历史，因此公开发布/正式提交仍不合规。
- Git 工作流：现有历史留在 `main`；正式编码开始采用 branch + worktree + PR，此项此前偏离已解决。
- 仓库定位：早期探索表述已被正式定位覆盖，但历史记录不删除，以展示需求演化。

## 8. PLAN 与陌生智能体冷启动状态

- SPEC 批准：用户于 2026-08-08 明确回复“批准 SPEC”，`writing-plans` 门禁已解除。
- PLAN 状态：已使用 Superpowers `writing-plans` 生成根目录 `PLAN.md`，正在完成正式文档提交。
- 冷启动状态：已创建 Claude Code 隔离 worktree，但学校接口兼容配置尚未通过最小连通测试；Claude 尚未读取 SPEC/PLAN 或试做 task。
- 既定执行协议：使用与 Codex 不同类型的全新智能体，不提供对话或 memory，只给 SPEC + PLAN；令其选 1–2 个 task，遇到不确定立即暂停。
- 后续记录内容：智能体停顿点、错误解读、产出差距、缺陷归因，以及 SPEC/PLAN 修订前后的关键 diff。

## 9. PLAN 生成过程

### 9.1 文件结构与任务边界

- 选择一份总 PLAN，而不是按子系统拆成多个计划：凭据、工作区、治理、反馈、LLM、记忆最终必须由同一个 AgentLoop 闭环，属于一个集成产品。
- 将旧 `mini-harness` 原型迁移为 `src/fbw_harness` 包；根目录保留课程文档、CI、构建和证据文件。
- 拆成 14 个可独立评审 task：安全骨架、领域契约、配置、凭据、工作区、事务、治理、反馈、LLM/上下文、记忆、AgentLoop、CLI/演示、分发、最终门禁。
- Task 4/5/8/9/10 可在基础契约合并后并行；AgentLoop 在所有核心能力完成后统一集成。

### 9.2 TDD、worktree 与评审

- 每个 task 明确精确文件、消费/产出接口、失败测试、预期失败、最小实现、绿色验证和中文 Conventional Commit。
- 现有文档历史保留在 `main`；正式编码 task 使用独立 branch + worktree，经 SPEC 合规和代码质量两阶段评审后通过 PR 合并。
- PLAN 记录当前仓库没有远端时的停止条件，不猜测 NJU Git/GitHub URL。

### 9.3 风险门禁

- Task 1 只清理当前树旧原型中的明文，不重写已有 Git 历史。
- Task 14 明确历史扫描在当前决定下会失败：只报告 commit/path，不打印 Key，不创建公开 Release，也不把失败伪装成合规。
- 首版 WebUI 冲突继续保留；未来可扩展接口不等于已交付 WebUI。

### 9.4 PLAN 自检结果

- 14 个 task、88 个可勾选实现步骤、14 个 task commit；
- F-01 至 F-12、六个 harness 维度、三项机制演示和 AC-01 至 AC-24 均有追踪映射；
- 未发现未完成标记、占位内容或明文 Key 模式；
- 修正了测试片段中的隐式 helper，并明确未知工具由 ActionParser 拒绝、合法动作的危险路径由 PolicyEngine 拒绝。

## 10. 第一次陌生智能体冷启动尝试

### 10.1 隔离与基线

- 智能体类型：Claude Code 2.1.226，与主开发智能体 Codex 不同；
- 隔离位置：`D:\Codes\FBW-worktrees\cold-start-claude`；
- 分支：`cold-start/claude-spec-plan`，基于 `51a849d`；
- worktree 仅由 uv 创建了被忽略的 `.venv`，Git 跟踪文件保持干净；
- 基线能导入现有 `openai`，但旧原型尚未声明 pytest，因此 `python -m pytest` 以“No module named pytest”失败。这是 Task 1 将修复的已知起点，不是冷启动实现回归。

### 10.2 Claude 接口配置验证

用户指定学校 Anthropic 兼容 Base URL、临时 Key 和 `deepseek-v4-flash`。验证使用一次性进程环境、`--bare`、`--no-session-persistence` 和无工具模式，未把凭据写入文件或过程文档。

依次验证：

1. 直接把自定义模型名交给 Claude：被本地未知模型窗口校验拒绝；
2. 按错误提示关闭未知模型窗口校验：越过本地校验后返回通用“selected model”不可用；
3. 使用 Claude 原生 `sonnet` 槽位映射到学校模型：映射生效，但仍触发未知窗口校验；
4. 同时使用原生映射和窗口校验关闭：仍返回通用模型不可用。

### 10.3 当前结论与门禁

- 现有证据只能证明 Claude Code 的自定义模型识别与网关调用没有成功，不能据此否定用户已手动验证的 `/messages` 协议；
- 三种最小修正假设均未得到成功响应，按 `systematic-debugging` 停止继续叠加开关；
- Claude 尚未收到冷启动任务、没有读取 SPEC/PLAN、没有修改跟踪文件，因而不能伪造冷启动结果；
- 后续依据 DeepSeek 官方 Claude Code 接入文档重新验证，确认此前结论需要修正，详见 10.4。

### 10.4 官方配置复核与长任务限流

- DeepSeek 官方方案的关键变量是 `ANTHROPIC_AUTH_TOKEN`，并直接通过 `ANTHROPIC_MODEL` 等环境变量选择模型；这与 `--bare` 只接受 `ANTHROPIC_API_KEY` 的限制不兼容；
- Anthropic 客户端会在 Base URL 后追加 `/v1/messages`。学校给出的 `/v1` 适合 OpenAI 风格 Base URL，但 Claude Code 应使用学校域名根地址，避免形成重复的 `/v1/v1/messages`；
- 使用一次性进程环境、`--safe-mode`、`--no-session-persistence` 和无工具模式，`deepseek-v4-pro[1m]`、`deepseek-v4-flash[1m]` 均完成最小连接；因此 Claude Code 接入已验证成功；
- 随后仅提供 `SPEC.md`、`PLAN.md` 与 Task 2/5 指令，限制工具、禁止联网和 Git 写操作。第一次长请求约 221 秒后返回 `503`；降低推理强度后的第二次约 338 秒后明确返回系统容量限流；
- 两次长任务都没有修改跟踪文件，尚未形成可评价的 Task 2/5 产出。当前门禁阻塞归因为学校网关容量，而不是本机安装、认证、URL、模型名或手动/自动启动方式；
- 下一步是在低负载时按同一配置重试；若限流持续，再使用另一种陌生智能体执行相同 SPEC/PLAN 冷启动，不能把最小连通成功冒充为 Gate 2 完成。

### 10.5 冷启动有效反馈与 PLAN 修订

- 在后续重试中，学校网关恢复可用；第一次只读检查完成多轮工具调用后因 1 美元预算上限停止。只提高预算到 3 美元后，Claude Code 在约 71 秒内完成并返回 `COLD_START_BLOCKED`；
- 陌生智能体只依据 `SPEC.md`、`PLAN.md` 和当前 worktree，确认仓库仍处于 Task 1 前：`src/fbw_harness`、测试目录和 Task 1/2/3/5 产物均不存在，旧单文件原型仍在；
- 阻塞反馈经人工核验成立：PLAN 依赖图要求 Task 1 → Task 2 → Task 3，而原 Gate 2 却要求从基线直接试做 Task 2 与 Task 5；Task 5 还明确消费 Task 2 与 Task 3 的产物；
- 缺陷归因：不是 SPEC 功能规约缺失，也不是陌生智能体能力问题，而是主 Codex 编写 PLAN 时选择了两个“有代表性”的任务，却忽略了冷启动 worktree 的实际起点；
- 修订：Gate 2 改为从基线按顺序试做 Task 1 → Task 2；Task 5 回到正式实现阶段，在 Task 2、Task 3 合并后执行。Gate 1 与客观缺陷记录标记完成，Gate 4 等待用户重新批准修订版 PLAN；
- 安全观察：只读冷启动没有修改文件或提交；Claude 为核验原型状态读取了旧源码并发现已有明文凭据，这再次说明正式公开发布前的凭据门禁仍然必要。
- 用户于 2026-08-09 明确回复“批准”，同意 Gate 2 改为 Task 1 → Task 2；Gate 4 因此完成，可以重新执行写入型冷启动试做。该批准只解除冷启动门禁，不代表允许把试做产物合并到正式分支。

### 10.6 写入型冷启动发现 Windows 路径解码缺陷

- 写入型试做首次因外层 Codex 只允许写项目根目录、而冷启动 worktree 位于兄弟目录而统一触发 `EPERM`；在保持 Claude 工具白名单、不启用危险跳权的前提下，经明确授权在外层放行后，Write/Edit/子进程恢复正常；
- Claude 按 Task 1 创建测试、修改项目配置、更新锁文件、删除三个旧原型并创建包骨架和扫描脚本；在 10 美元预算门禁前尚未进入 Task 2，也未提交或合并；
- 人工按 PLAN 重跑 Task 1 验证：包版本测试通过、ruff 通过，凭据扫描测试稳定失败。失败发生在读取 `通用要求.md` 时，而文件和 Git 索引均实际存在；
- 编码诊断确认 `subprocess.run(..., text=True)` 使用 Windows 默认代码页错误解码 Git 输出的 UTF-8 中文路径：Git 结果字符串码点与文件系统实际名称不同；
- 缺陷归因：PLAN 的测试片段没有显式定义 Git 路径输出编码，违反 Windows 优先目标。修订为 `git ls-files -z` 返回原始字节，再逐项严格 UTF-8 解码；NUL 分隔也避免文件名换行造成歧义；
- Gate 2 仍未完成，Task 1 尚未绿色，Task 2 未开始。Gate 4 重新打开，等待用户批准此项 PLAN 修订后才允许 Claude 继续。
- 用户于 2026-08-09 再次明确回复“批准”，接受 `git ls-files -z` 与逐路径 UTF-8 解码修订；Gate 4 再次完成，可以保留当前未提交试做产物继续 Task 1，而不是从头重做已验证过的 RED 阶段。

### 10.7 Task 1/2 完成报告与独立评审

- Claude 在最新版 PLAN 下完成 Task 1 与 Task 2，自报 `COLD_START_COMPLETE`；主 Codex 随后独立重跑专项与全量验证，得到 Task 1 2/2、Task 2 17/17、全量 19/19、ruff 通过、正常仓库扫描退出 0；
- 按 `requesting-code-review` 派出的独立只读 reviewer 不读取本窗口推理，只对照 SPEC/PLAN、基线与未提交工作树。评审判定无 Critical，但有四项 Important，因此 Gate 2 不能仅凭绿色测试通过；
- 主 Codex 逐项复现：扫描脚本从非 Git 目录运行时打印 fatal 却返回 0；嵌套 `api_key` 可进入 RunEvent；调用者后续修改原 Mapping 会改变已构造事件；`RawDecision()`、不完整 `RunResult`、无 path 的 READ_FILE 均被接受；
- 缺陷归因同时包含 PLAN 与实现：PLAN 没有为扫描器错误退出、递归秘密、深度不可变和完整 Action 不变量提供足够测试；Claude 又偏离固定 RawDecision/RunResult 签名，并提前把 ApplicationService 定义成 Protocol；
- 对 reviewer 建议作了职责收敛：Action 模型只校验分类型必填字段；未知 JSON 字段和 finish 只允许 reason 仍由 Task 9 Parser 负责，避免重复解析职责；
- PLAN 集中修订 Task 1/2 的安全负例、模型冻结、固定签名、五个 Protocol 边界和 README 链接。Gate 4 再次打开，冷启动 worktree 保留但不提交、不合并，等待用户批准后再修复并复审。
- 同一 reviewer 复核修订版 PLAN 后确认架构职责无新阻断，但指出 fail-closed 退出码/错误摘要和 Action 全部必填字段仍未被 RED 测试精确锁定；随后补成精确退出码 `2`、固定 stderr、create/edit/finish 参数化负例，并增加非字符串键/不支持对象代表测试；
- 为避免深度不可变与后续 JSONL 冲突，明确只读 Mapping 留在领域层，Task 12 输出边界负责递归转换为普通 JSON 容器，不能反向削弱模型不可变性。

### 10.8 重启恢复检查与集中修订批准

- 电脑重启后先做恢复审计：`main` 仍指向集中修订提交 `e09cb62`，冷启动分支仍指向 `b04511a`，Task 1/2 的未提交文件与删除记录完整，两个 Git 目录均无 `index.lock`，也没有遗留 Claude、uv 或 Python 任务；
- 主 Codex 在保留现场上重新运行全量测试、ruff 和正常路径凭据扫描，结果仍为 `19 passed`、`All checks passed!`、扫描退出 `0`，证明重启没有破坏既有冷启动产物或验证环境；
- 用户明确表示：若恢复检查无影响，即批准集中修订后的 `PLAN.md`。上述条件已满足，因此本轮将 Gate 4 标记完成，不再重复索取批准；
- 该批准覆盖 `e09cb62` 中的 fail-closed 扫描、模型深度不可变、Action 必填字段、固定签名、Protocol 边界和 JSONL 转换边界。它不代表 Gate 2 已通过，也不授权提交或合并冷启动代码；下一步仍须让陌生 Claude 先观察新增测试失败，再修复并接受独立复审。

### 10.9 集中修订复验与 Gate 2 完成

- Claude 按批准版 PLAN 对第一轮 4 个 Important 问题补测试并修复，自报 37 项通过；主 Codex 独立得到 `37 passed`、ruff 通过、正常扫描退出 `0`、非 Git 目录扫描精确退出 `2` 且只输出固定摘要；
- 独立 reviewer 继续发现 3 个 Important：真正找不到 Git executable 时仍泄露原生错误并退出 `1`；空/default payload 跳过冻结；除 ApprovalRequest 外的 tuple/frozenset 字段未防御规范化。主 Codex 逐项最小复现，并确认它们违反 PLAN 已有文字，而不是新需求；
- 第二次 Claude 修复因内部 Bash 审批未能运行测试，却已经修改生产代码，因此不能冒充 TDD 完成。主 Codex按 `test-driven-development` 保留新增测试、撤回该轮生产修复，重新观察 Git 缺失测试 1 项失败、模型选择组 11 项失败/1 项既有行为通过，再重新应用最小修复；
- 最终独立验证为 `50 passed` 且无 warning、ruff `All checks passed!`、正常扫描退出 `0`；PATH 仅含 PowerShell、Git 真缺失时退出 `2`、stdout 空、stderr 精确为 `secret scan failed`；
- 同一 reviewer 第三次只读复核报告 0 Critical、0 Important、0 Minor，确认变更范围只含 Task 1/2、五个 Protocol 边界和全部不可变字段契约。Gate 2 至此完成；清理前确认临时分支无独有 commit，随后移除可舍弃 worktree 和 `cold-start/claude-spec-plan` 分支，试做产物没有提交或合并。
